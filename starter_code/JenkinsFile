pipeline {
 agent any
 stages {
     stage('build') {
         tools{
             maven 'maven'
         }
         steps {
             sh 'mvn -f Final_eCommerce-application_forUdacity/starter_code/pom.xml clean install -DskipTests'
         }
     }
 }
}
