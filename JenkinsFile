pipeline {
 agent any
 stages {
     stage('build') {
         tools{
             maven 'maven3.2.1'
         }
         steps {
             sh '{MAVEN_HOME}/bin/mvn --version'
         }
     }
 }
}
